@startuml
!define RECTANGLE class
skinparam defaultTextAlignment center
skinparam backgroundColor white
skinparam shadowing false

skinparam activity {
  BackgroundColor white
  BorderColor black
  FontName Arial
  FontSize 11
}

start
:Ввод параметров:
- sheetId
- baseName|

if (Репозиторий открыт?) then (нет)
  :Возврат пустой строки|
  stop
else (да)
endif

:Получить данные ведомости:
findGradeSheet(sheetId);

if (Ведомость найдена?) then (нет)
  :Возврат пустой строки|
  stop
else (да)
endif

:Извлечь данные:
- groupId
- disciplineName
- createdAt;

:Получить список студентов группы:
listStudents(groupId);

:Создать HTML-документ:
<!DOCTYPE html>;

:Добавить заголовок:
- Министерство науки
- Название учреждения
- "Аттестационная ведомость"|

:Добавить информацию:
- Дисциплина: {disciplineName}
- Группа: {groupId}
- Дата: {createdAt}|

:Создать таблицу:
<table> с колонками:
№, ФИО, Допуск, Оценка, Подпись;

:Установить idx = 1;

while (Для каждого студента) is (есть)
  :Добавить строку таблицы:
  - № п/п: idx
  - ФИО: student.fio
  - Допуск: student.status
  - Оценка: пусто
  - Подпись: пусто|
  
  :idx++;
endwhile (нет)

:Закрыть таблицу </table>;

:Добавить подвал:
- Примечание об оценках
- Подпись преподавателя
- Подпись зам. директора|

:Добавить кнопку печати:
<button onclick="window.print()">;

:Получить путь к рабочему столу:
QStandardPaths::DesktopLocation;

:Сформировать имя файла:
{desktopPath}/{baseName}.html;

:Открыть файл на запись:
QFile::WriteOnly;

if (Файл открыт?) then (нет)
  :Возврат пустой строки|
  stop
else (да)
endif

:Записать HTML в файл;

:Закрыть файл;

:Возврат полного пути к файлу|

stop

@enduml
